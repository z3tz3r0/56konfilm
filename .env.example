# Test Environment Configuration
TEST_ENV=local
BASE_URL=http://localhost:3000

# Authentication (If added later)
TEST_USER_EMAIL=admin@56konfilm.com
TEST_USER_PASSWORD=

# Feature Flags / Mode Context
# Default mode for tests if not switched
DEFAULT_SITE_MODE=commercial # commercial | wedding
